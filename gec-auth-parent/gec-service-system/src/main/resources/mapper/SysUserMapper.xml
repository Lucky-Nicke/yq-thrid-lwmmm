<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanxige.mapper.system.SysUserMapper">
    <resultMap id="SysUserMap" type="com.lanxige.model.system.SysUser" autoMapping="true"></resultMap>

    <sql id="columns">
        id
        ,username,password,name,phone,head_url,description,status,create_time,update_time,is_deleted
    </sql>

    <select id="selectPage" resultMap="SysUserMap">
        select
        <include refid="columns"></include>
        from sys_user
        <where>
            <if test="vo.keyword != null and vo.keyword != ''">
                and (username like CONCAT('%',#{vo.keyword},'%') or name like CONCAT('%',#{vo.keyword},'%') or phone
                like CONCAT('%',#{vo.keyword},'%'))
            </if>
            <if test="vo.createTimeBegin != null and vo.createTimeBegin != ''">
                and create_time >= #{vo.createTimeBegin}
            </if>
            <if test="vo.createTimeEnd != null and vo.createTimeEnd != ''">
                and create_time &lt;= #{vo.createTimeEnd}
            </if>
            and is_deleted = 0
        </where>
        order by id desc
    </select>

    <!--  查询用户趋势  -->
    <select id="selectUserTrend" resultType="com.lanxige.model.vo.DayCountVo">
        SELECT
            DATE (create_time) AS day, COUNT(*) AS count
        FROM sys_user
        WHERE
            is_deleted = 0
          AND create_time >= DATE_SUB(CURDATE()
            , INTERVAL 6 DAY)
        GROUP BY DATE (create_time)
        ORDER BY day ASC
    </select>

    <!--  查询用户总数  -->
    <select id="selectTotalUserCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM sys_user
        WHERE is_deleted = 0
    </select>
</mapper>
